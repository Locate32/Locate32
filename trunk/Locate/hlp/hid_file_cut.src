TITLE=Open (File Context menu)
CONTENT=
<div id="menucommands">
<H1><A NAME="hid_file_cut"></A>Cut (File Context menu)</H1>

<P>Use this command to copy select files to clipboard. The selected files moved when 
a file operation is executed (e.g., by using Windows Explorer). 

This command is not visible if no file is selected.</P>

<P>Copying data to the clipboard replaces the contents previously stored there.</P>

</div>

